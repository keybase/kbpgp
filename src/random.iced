openpgp = require 'openpgp'
{prng} = require 'triplesec'

#================================================================


generate = (n_bits, cb) ->


generate_prime = (n_bits, p, cb) ->


#================================================================

exports.generate = generate
exports.generate_prime = generate_prime