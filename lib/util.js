// Generated by IcedCoffeeScript 1.6.3-g
(function() {


  exports.make_time_packet = function(d) {
    var b;
    d || (d = Math.floor(Date.now() / 1000));
    b = new Buffer(4);
    b.writeUInt32BE(d, 0);
    return b.toString('binary');
  };

  exports.uint_to_buffer = function(nbits, i) {
    var b;
    switch (nbits) {
      case 16:
        b = new Buffer(2);
        return b.writeUInt16BE(i, 0);
      case 32:
        b = new Buffer(4);
        return b.writeUInt32BE(i, 0);
      case 8:
        b = new Buffer(1);
        return b.writeUInt8(i, 0);
      default:
        throw new Error("Bit types not found: " + nbit);
    }
  };

}).call(this);
