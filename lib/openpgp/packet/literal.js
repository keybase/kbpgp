// Generated by IcedCoffeeScript 1.7.1-c
(function() {
  var C, Literal, LiteralParser, OutStream, Packet, asymmetric, iced, uint_to_buffer, __iced_k, __iced_k_noop, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  iced = require('iced-runtime').iced;
  __iced_k = __iced_k_noop = function() {};

  _ref = require('./base'), OutStream = _ref.OutStream, Packet = _ref.Packet;

  C = require('../../const').openpgp;

  asymmetric = require('../../asymmetric');

  uint_to_buffer = require('../../util').uint_to_buffer;

  Literal = (function(_super) {
    __extends(Literal, _super);

    Literal.TAG = C.packet_tags.literal;

    Literal.prototype.TAG = Literal.TAG;

    function Literal(_arg) {
      this.format = _arg.format, this.filename = _arg.filename, this.date = _arg.date, this.data = _arg.data;
      Literal.__super__.constructor.call(this);
    }

    Literal.parse = function(slice) {
      return (new LiteralParser(slice)).parse();
    };

    Literal.prototype.toString = function() {
      return this.data.toString(this.buffer_format());
    };

    Literal.prototype.buffer_format = function() {
      switch (this.format) {
        case C.literal_formats.text:
          return 'ascii';
        case C.literal_formats.utf8:
          return 'utf8';
        default:
          return 'binary';
      }
    };

    Literal.prototype.to_signature_payload = function() {
      return Buffer.concat([this.data]);
    };

    Literal.prototype.write_unframed = function(cb) {
      var bufs, ret;
      this.filename || (this.filename = new Buffer([]));
      bufs = [new Buffer([this.format]), uint_to_buffer(8, this.filename.length), this.filename, uint_to_buffer(32, this.date), this.data];
      ret = Buffer.concat(bufs);
      return cb(null, ret);
    };

    Literal.prototype.to_literal = function() {
      return this;
    };

    return Literal;

  })(Packet);

  LiteralParser = (function() {
    function LiteralParser(slice) {
      this.slice = slice;
    }

    LiteralParser.prototype.parse = function() {
      var data, date, filename, format, k, known_formats, v;
      known_formats = (function() {
        var _ref1, _results;
        _ref1 = C.literal_formats;
        _results = [];
        for (k in _ref1) {
          v = _ref1[k];
          _results.push(v);
        }
        return _results;
      })();
      format = this.slice.read_uint8();
      if (__indexOf.call(known_formats, format) < 0) {
        throw new Error("unknwon format: " + format);
      }
      filename = this.slice.read_string();
      date = this.slice.read_uint32();
      data = this.slice.consume_rest_to_buffer();
      return new Literal({
        format: format,
        filename: filename,
        date: date,
        data: data
      });
    };

    return LiteralParser;

  })();

  exports.OutStream = OutStream = (function(_super) {
    __extends(OutStream, _super);

    function OutStream(_arg) {
      var header;
      header = _arg.header;
      OutStream.__super__.constructor.call(this, {
        header: header
      });
      this._did_write_header = false;
    }

    OutStream.prototype._write_header = function(cb) {
      var err;
      err = null;
      return cb(err);
    };

    OutStream.prototype._transform = function(buf, encoding, cb) {
      var ___iced_passed_deferral, __iced_deferrals, __iced_k;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      __iced_deferrals = new iced.Deferrals(__iced_k, {
        parent: ___iced_passed_deferral,
        filename: "/Users/max/src/keybase/kbpgp/src/openpgp/packet/literal.iced",
        funcname: "OutStream._transform"
      });
      this._write_header(__iced_deferrals.defer({
        lineno: 92
      }));
      __iced_deferrals._fulfill();
    };

    OutStream.prototype._v_transform = function(buf, encoding, cb) {
      return this._ps.write(buf, encoding, cb);
    };

    OutStream.prototype._v_flush = function(cb) {
      return this._ps.end(cb);
    };

    return OutStream;

  })(base.OutStream);

  exports.Literal = Literal;

}).call(this);
