// Generated by IcedCoffeeScript 1.6.3-g
(function() {
  var K, SHA512, alloc, pack, sign, verify, _ref;



  _ref = require('./hash'), SHA512 = _ref.SHA512, alloc = _ref.alloc;

  K = require('./const').kb;

  pack = require('./kbpacket/encode').pack;

  sign = function(_arg, cb) {
    var body, hash, header, key, output, payload, progress_hook, sig, type;
    key = _arg.key, type = _arg.type, body = _arg.body, hash = _arg.hash, progress_hook = _arg.progress_hook;
    if (hash == null) {
      hash = SHA512;
    }
    header = {
      type: type,
      version: K.versions.V1,
      hash: hash.type,
      padding: K.padding.EMSA_PCKS1_v1_5
    };
    payload = pack({
      body: body,
      header: header
    });
    sig = key.pad_and_sign(payload, {
      hash: hash
    });
    output = {
      header: header,
      sig: sig
    };
    return cb(null, output);
  };

  verify = function(_arg, cb) {
    var body, err, hash, hd, header, key, payload, progress_hook, sig, type;
    type = _arg.type, key = _arg.key, sig = _arg.sig, body = _arg.body, progress_hook = _arg.progress_hook;
    header = hd = sig.header;
    payload = pack({
      body: body,
      header: header
    });
    hash = alloc(header.hash);
    err = hd.version !== K.versions.V1 ? new Error("unknown version: " + header.version) : hd.padding !== K.padding.EMSA_PCKS1_v1_5 ? new Error("unknown padding: " + header.padding) : type !== hd.type ? new Error("Unexpected sig type; wanted " + type + ", got " + hd.type) : key.verify_unpad_and_check_hash(sig.sig, payload, hash);
    return cb(err);
  };

  exports.sign = sign;

  exports.verify = verify;

}).call(this);
